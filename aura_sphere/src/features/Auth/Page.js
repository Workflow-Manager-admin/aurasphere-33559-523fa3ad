import React, { useState } from "react";
import { useNavigate } from "react-router-dom";
import { useAuth } from "./AuthContext";

/**
 * AuthPage legacy component stub for compatibility (no longer used for modal/UI).
 */
function AuthPage() {
  // This is intentionally left empty since AuthCard is now used by SplashScreen.
  return null;
}

export default AuthPage;
